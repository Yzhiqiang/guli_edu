<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yu.edu.mapper.CourseMapper">
    <select id="getPublishCourseInfo" resultType="com.yu.edu.entity.vo.CoursePublishVo">
        select ec.id, ec.title, ec.price, ec.lesson_num as lessonNum, ec.cover,
               et.name as teacherName,
               es.title as subjectLevelOne,
               es1.title as subjectLevelTwo
        from edu_course ec left join edu_teacher et on ec.teacher_id = et.id
                           left join edu_subject es on ec.subject_parent_id = es.id
                           left join edu_subject es1 on ec.subject_id = es1.id
        where ec.id=#{id}
    </select>
</mapper>
